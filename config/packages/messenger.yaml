framework:
    messenger:
        # failure_transport: failed

        transports:
            async: '%env(MESSENGER_TRANSPORT_DSN)%'
#            failed: 'doctrine://default?queue_name=dead_letter'
            sync: 'sync://'

        routing:
            Ferdyrurka\YourFeed\Application\Command\ImportPostCommand: async
